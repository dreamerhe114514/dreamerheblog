if hexo-config('preloader.enable')
  if hexo-config('preloader.load_style') == 'fullpage-loading'
    @import './_load_style/fullpage-loading'
  else if hexo-config('preloader.load_style') == 'icat'
    @import './_load_style/icat'